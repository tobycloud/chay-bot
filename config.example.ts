import { Config } from "bot";

const config: Config = {
  bot: {
    token: "",
    prefix: "c!",
    owners: [""],
    managers: [""],
    channels: {
      error: "",
    },
  },
};

export default config;
